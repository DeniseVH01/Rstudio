suma<- function(x,y){
  resultado <- x + y
  return (resultado)
}
suma(x=4,y=6)

suma <- function(x,y) x+y
suma(x=4, y=6)

Area<- function(A,B){
  resultado <- (A*B)/2
  return(resultado)
}
Area(A=5,B=3)
